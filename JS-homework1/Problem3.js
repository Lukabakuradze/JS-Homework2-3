var array = [ 'watermelon', 'pear', 10, 45, 50, 'apple', 'ananas' ];

for (var i=0; i<array.length; i++ ){
    var element = array[i]
    if (typeof element == 'string'  ){
        console.log(element);

    }
}
